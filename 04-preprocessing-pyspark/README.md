# Module 04 — Data Preprocessing with PySpark (Colab-ready)

- Open Colab → Upload `preprocessing_colab.py` → run (or convert to notebook).
- Steps covered: Missing values, duplicates, normalization/standardization, aggregation, binning, date features, encoding, TF‑IDF.
