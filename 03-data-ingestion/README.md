# Module 03 — Data Ingestion: Sqoop, Flume, Kafka

Follow the scripts:
- `sqoop_import.sh` — move `employees` table from MySQL to HDFS.
- `netcat-logger.conf` + `flume_run.sh` — send lines via Netcat and log at sink.
- `kafka_quickstart.sh` — create topic, produce, and consume messages.
